<h1>New Post</h1>

<%= form_for @post do |f| %>

  <p>
  	<%= f.label :title %>
  	<%= f.text_field :title %><br>
  </p>
  <p>
  	<%= f.label :body %>
  	<%= f.text_area :body %><br>
  </p>
  <p>
    <%= f.submit "Submit" %>
  </p>
<% end %>